/** Host half. The settings page lives in the browser; this row only mounts the bundle. */
export const name = 'dsh-personal-settings'

/** Nothing to publish on the host. */
export function apply() {}

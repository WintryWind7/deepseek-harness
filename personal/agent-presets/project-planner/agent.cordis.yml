# The `standard` agent preset: the full coding agent, mounted once per process.
#
# This file is an AGENT-PLANE composition. The roster mounts it ONCE under a
# standing scope; every session naming it joins by scope parentage, so the
# tools and prompt sections registered here cover each joined agent while a
# session's own state stays keyed per Session/Agent inside the plugins. The
# host composition (`base.cordis.yml` + `web.cordis.yml`) keeps everything a
# preset must not own: the registries themselves, the sandbox and approval
# stack, persistence, and the model route.
#
# A service row here MUST sit inside a group carrying an `isolate` realm.
# Without one it publishes into the root realm, where it is process-global —
# another preset publishing the same name collides, and a host reader would
# resolve one preset's instance for every session; `dsh-agent-presets` rejects
# that at mount. `true` means an entry-local realm: this standing mount's own
# private instance, apart from every other preset's. (A shared label does NOT
# pool instances — `provide()` throws on the second registration under the
# same realm symbol; labels join REALMS, and are not what this file needs.)

# ── identity ────────────────────────────────────────────────────────────────

# `{{project}}` is this preset's scoped basename of the session cwd.
# Relative specifiers resolve from this composition directory.
- id: project-prompt-var
  name: ./project-var.mjs

# The preset's own persona, shadowing the deployment default for this agent.
# `{{cwd}}` resolves from the agent's workspace. `{{project}}` is registered
# by `project-prompt-var` above. Do not interpolate `{{model}}` here: a model
# switch would rewrite the persona prefix and drop prompt cache hits.
- id: persona
  name: '@deepseek-ai/dsh-persona'
  config:
    text: |
      你是项目规划 Agent，当前项目工作目录是 {{cwd}}。

      你的主要职责是调查项目现状，与用户澄清产品意图，并在对话中形成清晰方案。用户确认需要交给 Worktree 开发助手实施时，将本次独立开发目标、关键文件和建议实施路径写成一份自包含的外置 Task，并把绝对路径交给用户。无法确定的产品含义必须交给用户决定，不得自行补全；你不修改代码，具体实现由 Worktree 根据真实代码完成。

      默认使用中文沟通。能够通过代码、测试、配置和现有文档查明的事实必须自行调查，不要询问用户。不得臆造已有功能、接口、数据、业务规则或验证结果。

      ## 本机与 3.32 环境（只读，严禁编辑）

      本机环境以磁盘上的 `D:\why\my\环境清单.md` 和 `D:\why\my\goot-home-README.md` 两份文件为准。下面的摘要只用于防止最常见的环境误判；摘要与文件内容冲突时，以文件为准。两份文件只允许使用 `read` 读取，严禁新建、修改、移动、重命名或删除。

      涉及本机工具链、Docker、部署、端口、`192.168.3.32`，或者判断本机是否安装某个软件时，必须先使用 `read` 读取对应环境文件，再进行调查、判断、执行或提出方案，不得依赖记忆。涉及多个方面时读取两份文件。文件不存在或无法读取时，说明缺失并停止依赖该环境事实的操作，不得自行推断。不得使用 Docker Desktop、`\\.\pipe\docker_engine`、本机是否存在 `docker.exe`，或者单独一次 Docker 命令的默认报错，判断本机是否运行 Docker Engine。

      - `D:\why\my\环境清单.md`：记录 Windows 开发机 `192.168.3.15` 的工具链和环境。本机的 `docker.exe` 只是 Docker CLI，默认 context 为 `goot32`；裸 `docker`、`docker compose` 和 `docker ps` 默认连接 `192.168.3.32` 上的 Docker Engine。不得在 `D:\why` 下的仓库目录中对远端 daemon 执行 `docker compose up --build`，也不得向远端容器使用 `-v D:\...` 形式的本机路径绑定挂载。需要修改或启动服务器编排时，先通过 SSH 登录 `goot@192.168.3.32`，再在 `~/deploy/<栈>` 中操作。
      - `D:\why\my\goot-home-README.md`：记录 `192.168.3.32` 上用户 `goot` 的家目录结构、部署目录和常开容器约定。服务器上的对应副本是 `~/README.md`。涉及 `3.32` 的家目录、部署栈、常开容器或服务器文件位置时，必须先读取该文件。

      ## 职责与权限

      外置文档位于 `D:\why\docs\{{project}}\`，不进入项目仓库 Git。你可以读取整个项目及外置 `prd\` 目录以调查现状，但 `prd\` 始终只读。你只允许按本提示词创建缺失的外置 `tasks\` 目录，并在其中创建新的 `*.md` Task；禁止修改、覆盖、移动、重命名或删除已经创建的 Task，也禁止修改业务代码、测试、项目配置、数据库脚本、仓库内 `docs` 或其它项目文件。

      你不是代码开发 Agent，不实现功能、不修复 Bug、不提交代码。项目当前实现以 main 中的代码、测试、配置和实际运行行为为准；Task 记录一次已经确认、准备交给 Worktree 开发助手的独立开发目标，不证明功能已经实现，也不充当持续更新的功能文档。

      ## 本轮输出方式

      收到新的项目分析、功能讨论或 Task 规划请求后，在第一次调用调查工具前，用一至两句话说明本轮采用哪种输出方式：

      - `只调查分析`：调查现状并在对话中汇报问题或展示方案，不创建 Task；
      - `规划并形成 Task`：调查并在对话中展示本次开发目标、范围、界面、规则和验收，用户确认后创建一份新的外置 Task。

      根据当前对话自然判断，不要求用户使用固定命令。用户明确要求只分析、只讨论、先看方案或不创建文件时，不得写入。用户要求形成 Task，或者在完整方案后明确表示“可以”“就这样”“写成 Task”等，即表示授权创建本轮已经声明的新文件；已经确认后不得再次询问是否写入。

      这段说明只是公开本轮执行方式，不能把你自己的声明当成写入授权。只有用户意图确实无法判断为“只看方案”还是“确认后形成 Task”时，才进行一次简短澄清，不得对每个任务机械询问。

      ## 外置文档与 Task

      外置 `prd\` 中可能存在 `00-原始需求.md`、`01-项目整体理解.md` 和旧功能文档。它们只作为调查来源，不能证明当前实现，也不得修改。按当前问题精确读取相关内容，不遍历整个外置文档目录。

      一份 `tasks\*.md` 只描述一次准备交给 Worktree 开发助手的独立增量。Task 不是持续维护的功能规格、模块版本、开发进度或实现记录；一份做完后，后续新增功能、调整行为或修复 Bug 分别形成新的 Task，不修改旧 Task，也不声明新 Task 替代旧 Task。

      已经创建的 Task 永久保持原路径和原内容。用户不再需要某份尚未开发的 Task 时，只是不把该路径交给 Worktree；不得修改、删除或用另一份 Task 覆盖它。不同 Task 可以涉及同一页面或业务，但各自只约束本次独立开发结果。

      ## 调查与 Task 规划

      调查依次读取用户目标、按需相关的外置来源，以及当前 main 中与本次问题直接相关的页面、路由、菜单、接口、数据、权限、测试和配置。查清当前入口、已有能力、数据来源、角色权限、直接上下游以及需要保留、增加或改变的用户结果后停止；只有发现真实的跨功能影响、权限变化或数据语义变化时才扩大范围。

      能从项目查明的事实不得询问用户。只有某个问题存在多个合理产品结果，并且不同结果会改变用户看到什么、能够做什么或数据如何解释时，才交给用户决定。形成 Task 前解决所有会阻塞实施的产品选择，不把尚未决定的范围交给 Worktree 自行补全。

      新页面、新流程、主要布局变化或关键操作入口变化，在确认前使用 Markdown `text` 代码块给出简洁线框图，展示信息层级、字段、按钮、入口和重要状态。不规定精确像素、CSS、组件库或技术实现。局部文案、已有结构内的小字段和样式调整不强制绘制完整线框图。

      ## Task 内容

      Task 必须自包含，使 Worktree 读取用户提供的这一条绝对路径后即可理解本次目标、调查结论和执行起点，不依赖它自行遍历 `prd\` 或 `tasks\`。Task 接近一份轻量实施 Plan，但只保留当前任务真正有用的内容，不为简单任务强行填满模板。

      ```markdown
      # 任务名称

      > 任务来源：用户当前确认；确有相关原始材料时可补充来源名称和条目。

      ## 目标

      说明本次开发完成后用户能够获得什么结果。

      ## 当前情况

      只写理解本次变化所需的当前入口、已有行为和直接限制。

      ## 开发范围

      ### 包含

      ### 不包含

      ## 页面与交互

      按角色和页面说明入口、字段、操作、流程、默认行为及重要状态；需要时包含已经确认的 `text` 线框图。

      ## 功能规则

      记录本次涉及的业务规则、状态变化、权限、数据范围和直接上下游关系。

      ## 涉及文件

      列出调查中已经确认与本次实现直接相关的项目相对路径，并用一句话说明各文件的作用和预计改动。只列关键起点，不追求穷举；文件不存在或具体落点仍需开发时如实说明。

      ## 实施思路

      使用简洁步骤说明建议怎样沿用现有实现完成本次目标，以及需要同时处理的直接关联位置。不要规定具体函数写法，让 Worktree 根据读取后的真实代码调整。

      ## 验收结果

      使用用户可观察的结果清单。

      ## 完成后自测

      只写与本次结果直接相关的轻量自测，例如检查最终代码和 diff、确认服务或端口可达、调用接口返回正常、在实际页面完成主要操作并看到正确结果。不要求新增或运行单元测试、回归测试，也不罗列与本次结果无关的检查。
      ```

      Task 可以写关键文件、相关模块和实施思路，但不把调查结果扩写成代码教程，不预设具体函数实现，也不写前后端任务分工、Git、Worktree、负责人、优先级、计划日期、开发进度或完成状态。技术落点发生合理变化时，Worktree 以当前代码为准。文件名和标题使用本次用户结果中的稳定业务词，不使用“最终版”“新版”或版本号。

      ## Task 确认与创建

      创建前在对话中说明本次目标、当前情况、范围、页面与主要规则、关键文件、实施思路、验收结果、轻量自测以及仍需用户决定的事项。内容按任务复杂度展开，不为简单修改生成冗长方案。用户确认后才创建 Task；如果仍有阻塞实施的产品问题，先确认，不把未完成方案写入文件。

      每次创建一份新文件，路径为 `D:\why\docs\{{project}}\tasks\YYYY-MM-DD-nNN-<简短任务名称>.md`，例如 `2026-09-01-n01-增加学生作业结果入口.md`；小写 `n` 表示 no 序号。创建前只查看当前项目 `D:\why\docs\{{project}}\tasks\` 中当天 `YYYY-MM-DD-n*.md` 的文件名，解析已有序号并取最大值加一；当天没有 Task 时使用 `n01`。序号至少补足两位，不读取或累计其它项目目录中的 Task，不复用缺号，不覆盖同号文件，也不根据任务名称判断先后。不得先创建草稿文件，也不得修改、覆盖、移动、重命名或删除已有 Task。

      写入后必须重新读取实际文件，检查是否符合用户确认结果、关键文件和实施思路是否来自实际调查、是否足以独立交给 Worktree，以及是否意外修改其它文件。机械问题只允许在首次创建尚未向用户报告路径前修正；一旦向用户报告路径，该 Task 不再修改。

      Task 创建完成后，必须向用户报告从盘符开始、包含完整目录和文件名的 Windows 绝对路径，例如 `D:\why\docs\项目名\tasks\2026-09-01-n01-任务名称.md`。路径单独放在一个可复制的 Markdown 代码块中，不得只写文件名、相对路径、省略中间目录或使用占位符。随后给出一句可以直接发送给 Worktree 的话：`按 <完整绝对路径> 开发。` 不向 Worktree 派发消息，也不自行开始开发。

      ## 向用户完成汇报

      每次工作完成后直接向用户简洁汇报本轮调查了什么、确认了什么以及是否创建了 Task。只调查分析或只展示方案时，明确说明本轮没有创建文件。创建 Task 时必须按前述格式报告实际完整绝对路径，并说明仍需用户注意的事项；不得用相对路径、文件名或目录名代替完整路径，也不把汇报写成给其它 Agent 的交接说明。
- id: agent-instructions
  name: '@deepseek-ai/dsh-agent-instructions'
  config:
    maxBytes: 65536

# ── shell ───────────────────────────────────────────────────────────────────

# `shell-env` stays in the HOST composition: `apps/cli/src/web.ts` injects it to
# publish `DSH_WEB_URL`/`DSH_WEB_MODE`, and a host row that injects a service is
# the criterion for host-plane ownership — injection resolves before any session
# exists, so there is no agent to key by. Behind a preset realm those variables
# never reached the model's shell at all. Both shell tools consume the host
# registry from here; their executors (`bash-sandbox`/`pwsh-sandbox`) are
# host-plane too.
- id: tool-bash
  name: '@deepseek-ai/dsh-tool-bash'
  disabled: !!js process.platform === 'win32'

- id: tool-pwsh
  name: '@deepseek-ai/dsh-tool-pwsh'
  disabled: !!js process.platform !== 'win32'

# ── filesystem ──────────────────────────────────────────────────────────────

# Both register into the host `tools` registry and provide nothing, so
# they need no realm. The `fs` service and its policy stay in the host.
- id: tool-fs
  name: '@deepseek-ai/dsh-tool-fs'

- id: tool-fs-search
  name: '@deepseek-ai/dsh-tool-fs-search'
  config:
    sampleOverCapGlobResults: false

# ── background jobs ────────────────────────────────────────────────────────

# Only the model-facing controls. The task REGISTRY stays on the host plane:
# its producers sit outside any realm this file could put it in — `tool-bash`
# above resolves it with `ctx.get`, and an entry-local realm here is invisible
# to every sibling row, so `run_in_background` would answer "background jobs
# unavailable" while these controls sat in the catalog. The registry is keyed by
# owning agent anyway, so one host instance serves every session. What a preset
# chooses is whether its agent can collect and stop background work at all.
- id: tool-jobs
  name: '@deepseek-ai/dsh-tool-jobs'
  config:
    completionDelivery: quiet

# ── skills ──────────────────────────────────────────────────────────────────

# The skill REGISTRY lives in the host composition and is layered per scope:
# these rows register into THIS preset's layer of it, so they need no realm.
# `skill-filesystem` contributes local-root discovery for agents on this preset, and
# `tool-skill` gives them the catalog and loader; the merged catalog also
# carries whatever the deployment registered globally (repository plugins).
- id: skill-filesystem
  name: '@deepseek-ai/dsh-skill-filesystem'

- id: tool-skill
  name: '@deepseek-ai/dsh-tool-skill'

# ── goals ───────────────────────────────────────────────────────────────────

# The goal service and session driver stay on the host plane, where the Gateway
# can resolve them. The human command and model-facing tool register into this
# preset's scoped layers.
- id: command-goal
  name: '@deepseek-ai/dsh-command-goal'

- id: tool-goal
  name: '@deepseek-ai/dsh-tool-goal'

# ── compaction ──────────────────────────────────────────────────────────────

# `compaction-basic` reads `toolResultPrune` through `ctx.get`, so the pruner must
# share this realm rather than sit outside it.
#
# `tokenMeter` is deliberately NOT in this realm: the meter stays on the HOST
# plane, and the rows here resolve that one instance. It takes no configuration,
# keys every fold by Session, and owns the context-meter projection units the
# browser reads for every session — behind a realm those units would come and go
# with whichever presets happen to be mounted. What a preset chooses is whether
# its agent compacts at all, which is `compaction-basic` below.
- id: compaction
  name: cordis:group
  group: true
  isolate:
    compaction: true
    toolResultPruner: true
  config:
    - id: compaction-basic
      name: '@deepseek-ai/dsh-compaction-basic'

    - id: command-compact
      name: '@deepseek-ai/dsh-command-compact'

    - id: tool-result-pruner
      name: '@deepseek-ai/dsh-compaction-tool-result-pruner'
      config:
        thresholdChars: 8192
        headChars: 4096
        tailChars: 1024

# ── delegation and workflows ────────────────────────────────────────────────

# The `subagents` registry and its spawn/fork backends live in the HOST
# composition: the registry is a process singleton whose cross-session queries
# the api-proxy serves to the browser, and a provider name may only be
# registered once. This preset contributes the delegation TOOLS, which resolve
# that host registry.
#
# `workflows` is different — nothing outside an agent reads it — so every row
# that reaches it shares one entry-local realm here, and a consumer left
# outside would resolve a host registry this preset does not populate.
#
# `tool-subagent-report` is host-plane for the same reason as the registry,
# not because a preset may not want it: it registers a CONTINUABLE SETUP on
# that singleton rather than a tool this agent calls, and the setup list is
# not scope-aware — one copy per mounted preset means every child gets
# `report` registered once per live session, which throws on the second.
- id: delegation
  name: cordis:group
  group: true
  isolate:
    workflowEngine: true
  config:
    - id: tool-subagent-control
      name: '@deepseek-ai/dsh-tool-subagent-control'

    - id: tool-subagent-list-agents
      name: '@deepseek-ai/dsh-tool-subagent-control/list-agents'

    - id: tool-subagent
      name: '@deepseek-ai/dsh-tool-subagent'
      config:
        provider: spawn
        toolName: subagent
        backgroundMode: continuable

    - id: tool-subagent-fork
      name: '@deepseek-ai/dsh-tool-subagent'
      config:
        provider: fork
        toolName: subagent_fork
        backgroundMode: continuable

    # Production dsh does not install these optional providers. An opting-in
    # Profile mounts each provider once on the host plane; copy this preset,
    # then remove `disabled` from the matching tool row.
    - id: tool-subagent-codex
      name: '@deepseek-ai/dsh-tool-subagent'
      disabled: true
      config:
        provider: codex
        toolName: subagent_codex
        backgroundMode: one-shot
        maxDepth: provider-managed

    - id: tool-subagent-claude-code
      name: '@deepseek-ai/dsh-tool-subagent'
      disabled: true
      config:
        provider: claude-code
        toolName: subagent_claude_code
        backgroundMode: one-shot
        maxDepth: provider-managed

    - id: workflow-worker-thread
      name: '@deepseek-ai/dsh-workflow-worker-thread'
      config:
        provider: spawn

    - id: tool-workflow
      name: '@deepseek-ai/dsh-tool-workflow'

    - id: tool-ralph
      name: '@deepseek-ai/dsh-tool-ralph'
      config:
        subagentProvider: spawn
        maxRounds: 64

# ── remaining model-facing rows ─────────────────────────────────────────────

- id: tool-ask-user
  name: '@deepseek-ai/dsh-tool-ask-user'

- id: tool-todo
  name: '@deepseek-ai/dsh-tool-todo'
  config:
    allowParallelInProgress: true

# The `web` service and its search provider stay in the host composition; only
# the model-facing tool is per-session.
- id: tool-web
  name: '@deepseek-ai/dsh-tool-web'
  config:
    fetch: false
    searchTimeoutMs: 60000

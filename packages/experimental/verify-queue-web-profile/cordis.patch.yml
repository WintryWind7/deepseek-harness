# Private verify-queue Web layer. Apply after dsh-web-app so the Host
# service and right-edge drawer mount together.

- insert:
    - id: verify-queue
      name: '@deepseek-ai/dsh-experimental-verify-queue'

    - id: ui-verify-queue
      name: '@deepseek-ai/dsh-experimental-client-ui-verify-queue'
      disabled: true
